import { StringOperationPipe } from './string-operation.pipe';

describe('StringOperationPipe', () => {
  it('create an instance', () => {
    const pipe = new StringOperationPipe();
    expect(pipe).toBeTruthy();
  });
});
